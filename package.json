{
  "name": "site",
  "version": "1.0.0",
  "main": ".eleventy.js",
  "scripts": {
    "build:eleventy": "npx @11ty/eleventy",
    "watch:eleventy": "npx eleventy --serve",
    "build:sass": "npx sass src/sass:dist/css",
    "watch:sass": "npx sass --watch src/sass:dist/css",
    "start": "npm-run-all build:sass --parallel watch:*",
    "build": "npm-run-all build:*"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@11ty/eleventy": "^2.0.1",
    "prettier": "^3.3.3"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "sass": "^1.79.4"
  }
}
